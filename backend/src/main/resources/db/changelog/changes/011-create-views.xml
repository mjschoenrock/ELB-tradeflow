<?xml version="1.0" encoding="UTF-8"?>
<!--
011-create-views.xml
create two SQL views
-->
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="011-create-view-open-breaks" author="max">

        <createView viewName="v_open_breaks" replaceIfExists="true">
            SELECT rb.id AS break_id, rb.discrepancy_type, t.trade_ref, c.name AS counterparty
            FROM recon_breaks rb JOIN trades t ON t.id = rb.trade_id JOIN counterparties c ON c.id = t.counterparty_id
            WHERE rb.status='OPEN'
        </createView>

        <rollback>
            <dropView viewName="v_open_breaks"/>
        </rollback>

    </changeSet>

    <changeSet id="011-create-view-settlement-lag" author="max">
        <createView viewName="v_settlement_lag" replaceIfExists="true">
            SELECT t.trade_ref, t.trade_date, s.settled_at, (CAST(s.settled_at AS DATE) - t.trade_date) AS lag,
            s.status AS settlment_status FROM settlements S
            JOIN trades t ON t.id=s.trade_id WHERE s.status='SETTLED'
        </createView>

        <rollback>
            <dropView viewName="v_settlement_lag"/>
        </rollback>

    </changeSet>

</databaseChangeLog>